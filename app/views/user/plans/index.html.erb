		<h2 class="section-ttl">投稿一覧</h2>
		<div class="plans">
			<% @plans.each do |plan| %>
				<%= link_to plan_path(plan) do %>
					<h5><%= plan.plan_title %></h5>
				<% end %>
				  <p><%= plan.customer.name %></p>
					<p><%= plan.purpose_spot %></p>
					<p><%= plan.budget %> 円</p>
					<p><%= link_to "#{plan.post_comments.count} コメント", plan_path(plan.id) %></p>
			<% end %>
		</div>
		
		<!--タグリスト-->
<% @tag_list.each do |list|%>
<%=list.name %>
<%="(#{list.plans.count})" %><% end %>

投稿一覧画面で投稿に紐づくタグを表示
<% @plans.each do |plan| %>
<i class="fas fa-tag"><%= plan.tags.map(&:name).join(', ') %></i>